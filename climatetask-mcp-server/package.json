{
  "name": "climatetask-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for climate project task tracking, weather/climate data (Open-Meteo) and country development indicators (World Bank)",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "climatetask-mcp-server": "dist/index.js"
  },
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
